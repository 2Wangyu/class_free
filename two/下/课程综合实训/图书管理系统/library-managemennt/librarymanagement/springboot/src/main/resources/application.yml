# 为避免和前端的端口冲突，这里换了一个端口
server:
  port: 9090

# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library-management?serverTimezone=GMT%2b8 # beijingtime
    username: root
    password: ASD200404yw
    #改成自己的密码
mybatis:
  mapper-locations: classpath:mapper/*.xml